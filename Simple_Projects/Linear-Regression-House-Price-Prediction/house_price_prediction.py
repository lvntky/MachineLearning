# -*- coding: utf-8 -*-
"""house_price_prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1hainjbkMI4JNDhPP1Mr_384znNhY27ob

# House Price Prediction

## Import The Libraries
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

"""## Load the Data and Preprocessing"""

dataset = pd.read_csv('dataset/USA_Housing.csv')
dataset.head()

dataset.shape

#independet variables
x = dataset.iloc[:, 1:2]
#dependent variables
y = dataset.iloc[:, 5:6]

print(x)

print(y)

"""### Splitting Dataset into training and test sets"""

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size = 0.2, random_state = 0)

"""### Feature Scaling"""

print(x_train)
print('--------------------------------------')
print(y_train)

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()
x_train = sc.fit_transform(x_train)
x_test = sc.fit_transform(x_test)

"""## Training the Simple Linear Regression on train test"""

from sklearn.linear_model import LinearRegression
regressor = LinearRegression()
regressor.fit(x_train, y_train)

"""## Predicting the test result"""

y_pred = regressor.predict(x_test)

"""## Visualising the training set results"""

plt.scatter(x_train[:,0] , y_train, color = 'red')
#plotting the regression line
plt.plot(x_train, regressor.predict(x_train), color = 'blue')
plt.title('House Age - Price')
plt.xlabel('Age')
plt.ylabel('Price')
plt.show()

"""## Visualising the test set result"""

plt.scatter(x_test[:,0] , y_test, color = 'red')
#plotting the regression line
plt.plot(x_train, regressor.predict(x_train), color = 'blue')
plt.title('House Age - Price')
plt.xlabel('Age')
plt.ylabel('Price')
plt.show()

"""## Making single prediction on model"""

print(regressor.predict([[5]]))